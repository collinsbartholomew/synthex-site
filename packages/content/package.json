{
  "name": "@ai-site/content",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo content package build placeholder",
    "lint": "echo content package lint placeholder",
    "typecheck": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo",
    "update": "npx npm-check-updates@latest -u"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}

{
  "name": "@ai-site/observability",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo observability package build placeholder",
    "lint": "echo observability package lint placeholder",
    "typecheck": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo",
    "update": "npx npm-check-updates@latest -u"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}

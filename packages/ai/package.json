{
  "name": "@ai-site/ai",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo ai package build placeholder",
    "lint": "echo ai package lint placeholder",
    "typecheck": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo",
    "update": "npx npm-check-updates@latest -u"
  },
  "dependencies": {
    "@ai-site/content": "workspace:*",
    "@ai-site/db": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}

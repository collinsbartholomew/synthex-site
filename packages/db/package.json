{
  "name": "@ai-site/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo db package build placeholder",
    "lint": "echo db package lint placeholder",
    "typecheck": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo",
    "update": "npx npm-check-updates@latest -u"
  },
  "dependencies": {
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
